version: "3.8"
services:
  server:
    container_name: server-hompimpa
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT}:${PORT}"
    command: ["/app/main"]
